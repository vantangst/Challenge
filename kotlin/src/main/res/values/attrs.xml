<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="LightWeightImageView">
        <attr name="imgSrc" format="reference" />
        <attr name="imgPlaceHolder" format="reference" />
        <attr name="imgPlaceHolderColor" format="color" />
        <attr name="imgCircularProgress" format="boolean" />
        <attr name="imgPlaceHolderScaleType" format="enum">
            <enum name="matrix" value="0" />
            <enum name="fitXY" value="1" />
            <enum name="fitStart" value="2" />
            <enum name="fitCenter" value="3" />
            <enum name="fitEnd" value="4" />
            <enum name="center" value="5" />
            <enum name="centerCrop" value="6" />
            <enum name="centerInside" value="7" />
        </attr>
        <attr name="imgTransition" format="boolean" />
    </declare-styleable>

    <declare-styleable name="BottomMenuView">
        <attr name="tabMenu" format="reference" />
        <attr name="tabCustom" format="boolean" />
        <attr name="tabIconSelectedTint" format="reference" />
        <attr name="tabTextAppearance" format="reference" />
    </declare-styleable>

    <declare-styleable name="MenuHostFragment">
        <attr name="navMenu" format="reference" />
        <attr name="navType" format="enum">
            <enum name="order" value="1" />
            <enum name="stack" value="2" />
            <enum name="stackOrder" value="3" />
        </attr>
    </declare-styleable>

    <declare-styleable name="Destination">
        <attr name="navOrder" format="integer" />
        <attr name="navKeepInstance" format="boolean" />
    </declare-styleable>

    <declare-styleable name="CameraView">
        <!--
          Set this to true if you want the CameraView to adjust its bounds to preserve the aspect
          ratio of its camera preview.
        -->
        <attr name="android:adjustViewBounds" />
        <!-- Direction the camera faces relative to device screen. -->
        <attr name="facing" format="enum">
            <!-- The camera device faces the opposite direction as the device's screen. -->
            <enum name="back" value="0" />
            <!-- The camera device faces the same direction as the device's screen. -->
            <enum name="front" value="1" />
        </attr>
        <!-- Aspect ratio of camera preview and pictures. -->
        <attr name="aspectRatio" format="string" />
        <!-- Continuous auto focus mode. -->
        <attr name="autoFocus" format="boolean" />
        <!-- The flash mode. -->
        <attr name="flash" format="enum">
            <!-- Flash will not be fired. -->
            <enum name="off" value="0" />
            <!--
              Flash will always be fired during snapshot.
              The flash may also be fired during preview or auto-focus depending on the driver.
            -->
            <enum name="on" value="1" />
            <!--
              Constant emission of light during preview, auto-focus and snapshot.
              This can also be used for video recording.
            -->
            <enum name="torch" value="2" />
            <!--
              Flash will be fired automatically when required.
              The flash may be fired during preview, auto-focus, or snapshot depending on the
              driver.
            -->
            <enum name="auto" value="3" />
            <!--
              Flash will be fired in red-eye reduction mode.
            -->
            <enum name="redEye" value="4" />
        </attr>
    </declare-styleable>

    <declare-styleable name="RotateLoadingView">
        <attr name="loading_width" format="dimension" />
        <attr name="loading_color" format="color" />
        <attr name="shadow_position" format="integer" />
        <attr name="loading_speed" format="integer" />
    </declare-styleable>
</resources>